import { Board } from './Board';
import { Piece } from './Piece';
import { Square } from './Types';

export class Move {
  movePiece(piece: Piece, squareFrom: Square, squareTo: Square, board: Board): void {}
}
